# 书魂 BookSoul —— 项目选型与整体思路（草案）

> 项目选型与整体思路文档，v0.4 草案，作为选型记录与设计基线，后续持续修订。
> v0.2 变更：新增 §5「Token 优化策略」（含 DeepSeek 缓存降价发现、四层方案、四个创新点、上下文组装器）。
> v0.3 变更：新增 §11「Agent 架构模式选型」（六层认知地图、逐环节模式选型、**multi-agent 切分方案**、市面三大范式、误差雪崩与生产三件套）；明确「在线轻 / 离线重」「按角色切而非按字段切」两条核心决策。
> v0.4 变更：补齐 **§13 记忆三层契约**、**§14 人格设计与 mood 情绪系统**、**§15 MVP 范围**；§6 角色卡 Schema 补全为完整数据契约（Pydantic 骨架 + v2/v3 映射 + relations 方向）。**文档已达「MVP 可开工」完整度**（P0 待补项清零）。

## 0. 一句话定位

**把用户的小说 TXT 变成可插拔的「立体角色卡」，让书里的人活过来和你对话、走剧情。**

## 1. 为什么做这个（背景与差异化）

- **参考系**：猫箱（字节，社区 UGC + 开袋即食 + 移动端，下载 2.5 亿）、Flai（海外/繁体市场）、酒馆 SillyTavern（开源角色卡前端 + 自由度，32.9k★）。
- **核心问题**：猫箱靠社区 UGC 提供角色，个人项目冷启动没有社区。
- **差异化解法**：**内容导入替代社区 UGC**——用户自己带小说来，AI 从书里抽取立体角色。扮演"自己读过的角色"，情感代入远强于陌生 UGC 角色。
- **目标**：好玩、有意思、好上手；同时作为校招简历项目，具备可论证的 agent 技术深度，关联当前主流 agent 知识（记忆、上下文工程、RAG、多 agent、MCP、评估、安全）。

## 2. 产品形态与核心体验

产品闭环：**放 TXT（或导入现成角色卡）→ 离线生成立体角色卡 → 用户「调教」→ 可插拔对话 → 走原著线或 if 线。**

五个"好玩"引擎：

| 引擎 | 解决什么 | 技术载体 | 优先级 |
|---|---|---|---|
| 钩子 Hook | 第一秒入戏 | 角色卡 `first_mes`（书中名场面改写） | P0 |
| 戏剧 Drama | 不无聊、AI 主动推进 | 叙事调度器 + 角色 desire/flaw/secret | P1 |
| 关系 Relationship | 长期粘性、成长感 | 关系状态机（好感度/阶段/解锁） | P1 |
| 记忆 Memory | 长期体验的地基 | 三层记忆契约（canon/heuristic/reflex）——**具体设计待补（P0）** | P1 |
| 易上手 | 打开就能玩 | 角色即配置 + 预设/导入生态 | P0 |

好上手标准：**开场 3 句内，要么让人笑，要么让人好奇。**

## 3. 整体架构思路（借鉴 DSH 的插件化内核思想）

DSH（DeepSeek Harness）的架构本质：**用插件化内核，通过 patch 叠加 + 服务注入，组合成任意形态的 agent。** roleplay 的本质需求（同一对话内核组合出任意角色/世界观/记忆）与之同构。借鉴其思想、落地用 Python 轻量等价物，不复刻 Cordis。

四层架构：

```
内核层 Kernel —— 对话循环 + 上下文组装 + 模型路由(LiteLLM) + 状态机
角色层 Character —— 角色 = 配置叠加，可插拔角色卡；多角色 = 多份卡各自独立上下文
能力层 Services —— 记忆引擎 / 审核 guardrails / 工具（可插拔，记忆后续包成 MCP）
交付层 Entries —— Web(Chainlit→AG-UI) · REST(FastAPI) · 后续 ACP
```

**核心原则：离线构建 + 在线消费分离。** 分析一本书（抽取/建模/剧情理解）是一次性成本，产出结构化「角色资产包」；对话时按需注入是持续成本。此分界决定成本结构与缓存策略。

## 4. 核心数据流

```
离线管道（一次性，昂贵）——四阶段划分详见 §11.3：
TXT → 解析/分章/清洗（确定性代码，非 agent）
    → 阶段1 识别主要人物（workflow：逐章粗扫 → 候选列表）
    → 阶段2 按角色并行精抽个体属性（并行 map：同一抽取器 × N 角色）
    → 阶段3 全局关系与剧情理解（异构专家：关系图 / 事件时间线 / 剧情节点图）
    → 阶段4 聚合 + 校验（Verification 专家：对照原文验证）
    → 生成可插拔立体角色卡（JSON + 可选 PNG）

在线运行时（持续，轻量）：
用户消息 → 组装上下文（角色卡常驻 + 世界书注入 + 书中剧情 RAG + 原话 few-shot + 剧情摘要）
        → LLM 流式生成
        → 输出审核（反幻觉 / guardrails）
        → 记忆抽取更新（异步，不阻塞延迟）+ 剧情状态推进
        → 返回
```

## 5. Token 优化策略（成本与上下文的核心）

> token 成本是这类产品的命门（长对话 + 多层上下文 + 重度用户），必须从第一天设计进架构，而非事后优化。

### 5.1 核心思想：省 token 不是"砍内容"，是三个动作

1. **把稳定内容交给 prompt cache**（无损，省 ~90%）
2. **把会变的状态变成结构化数据**（不占自然语言 token）
3. **把用不上的内容按需注入**（渐进披露）

第 1、2 条是无损的，应从第一天做进架构；第 3 条是低损的；只有"摘要/压缩"是真正有损的，需要靠评估指标守住效果底线。

### 5.2 天时：DeepSeek 缓存降价（2026-09-10 生效）

DeepSeek Flash 系列调价落地，**缓存命中输入闲时 0.02 元/百万 token**，仅为未命中（1 元）的 **1/50**；输出闲时 4 元/百万。峰谷分时计费（高峰为工作日 9:00–12:00、14:00–18:00，价格翻倍）。**缓存自动生效、无需改代码**，只要求请求前缀一致。

→ 这让「DeepSeek 主力 + 角色卡稳定置顶」直接变成一个 **50 倍成本杠杆**。锁死「角色卡 → 系统提示 → few-shot → 世界书常驻」这段固定前缀，每一轮都白赚折扣。

**工程要点**：
- 固定 system / 角色卡**置顶**，不做动态修改——前缀一变，缓存全部失效。
- 长期不变的设定放前缀，动态内容（用户问题、检索结果）放**末尾**。
- 通过 usage 的 `prompt_cache_hit_tokens` / `prompt_cache_miss_tokens` 监控命中率，命中率持续偏低就是前缀在变。
- 批量任务（离线抽取、剧情分析）调度到闲时执行，再省一半。

### 5.3 四层优化方案

| 层级 | 方案 | 省多少 | 有损? | 依据 |
|---|---|---|---|---|
| 无损 · 第一天 | Prompt Cache 前缀工程 | 命中输入省 98% | 无损 | DeepSeek 自动缓存（09-10 降价） |
| 无损 · 第一天 | 稳定 / 变化分区排版 | 命中率最大化 | 无损 | 各家 cache 规则 |
| 无损 · 第一天 | 结构化状态外置 | 关系描述几百→几十 token | 无损 | 状态机（自研） |
| 无损 · 第一天 | 先天 / 后天记忆分层 | 不重复抽取书中剧情 | 无损 | 自研（项目核心） |
| 低损 · 早期 | 渐进披露（世界书触发） | 低频设定不常驻 | 低损 | SillyTavern / Anthropic |
| 低损 · 早期 | 记忆分层注入 top-k | 只注入相关条目 | 低损 | chronicler 三层契约 |
| 低损 · 早期 | RAG 精简 top-3~5 | 剧情注入 -60% | 有损可控 | 主流 RAG |
| 压缩 · 中期 | leanctx 分类压缩 | 输入 -40~60% | 有损可控 | leanctx / LLMLingua-2 |
| 压缩 · 中期 | 分层摘要（近期原文 + 远期摘要） | 历史 -50%+ | 有损 | 主流 compaction |
| 创新 · 中期 | 事件记忆图 | 剧情检索更准→注入更少 | 低损 | DREAM 事件图思路 |
| 创新 · 中期 | 叙事级摘要 | 摘要保戏剧张力 | 有损可控 | FictionRAG 元认知 |

### 5.4 每轮上下文的分层预算（目标值）

| 上下文层 | 每轮预算 | cache? | 说明 |
|---|---|---|---|
| 角色卡 + 系统提示 | 2-3K | ✅ | 稳定置顶 |
| few-shot 原话 | 0.5-1K | ✅ | 精选 1-2 例 |
| 世界书触发条目 | 0.5-1K | 部分 | 常驻部分 cache，触发部分后置 |
| 剧情 RAG | 1-1.5K | ❌ | top-k 最相关片段（初定 3~5，具体值由评估确定） |
| 记忆检索 | 0.5-1K | ❌ | 相关 + 可靠 top-k |
| 近期对话原文 | 4-8K | ❌ | 滚动窗口 |
| 远期剧情摘要 | 0.5-1K | ❌ | 分层摘要 |

### 5.5 四个切合场景的创新点

1. **事件记忆图（Event Memory Graph）**——替代"线性历史 + 摘要"。把剧情抽成**事件节点**（谁 + 做了什么 + 结果 + 与谁相关）+ 关系边，对话时按当前话题/角色/剧情节点检索最相关的几个事件注入。好处：关键事件不被摘要破坏（保持完整），token 只花在相关剧情上。**与「从小说抽剧情」的管道天然契合**：离线抽出的 `timeline / relations` 本来就是事件图。依据：DREAM。
2. **叙事级摘要（Narrative Summarization）**——不是"信息压缩"，是"戏剧连贯"。摘要时专门抽**伏笔、关系变化、情绪转折、未解决的冲突**，而非平铺流水账。依据：FictionRAG 的元认知思路。
3. **先天 / 后天记忆分层**——把最贵和最便宜的记住分开：**先天记忆**（书里剧情）走离线结构化抽取，保真（来自原文）、不占重新抽取成本；**后天记忆**（聊出来的）走异步增量抽取 + 三层契约。别人把两者一锅端进 mem0，又贵又易失真；本项目利用"从书抽取"的优势，只把贵的活留给后天记忆。
4. **分类压缩（叙事敏感度）**——借 leanctx「内容分类压缩」思想：寒暄/过渡可强压缩，**剧情转折/情绪爆发/伏笔必须 verbatim 保留**。用一个轻量分类器给历史对话打"叙事重要度"，重要的原样留、不重要的压缩。leanctx 的 LongBench v2 结果表明"分类压缩删 57% token 反而准确率翻倍"（盲目截断会删掉关键信息）。

### 5.6 架构组件：上下文组装器（Context Assembler）

把"组装上下文"做成**独立组件**，而不是散落各处：

```
Context Assembler
├─ 每层有独立配额（见 §5.4 预算表）
├─ 稳定层 / 变化层严格分区（决定 cache 命中率）
└─ token 计数器 + 超预算自动降级：
     先砍历史原文 → 再砍 RAG 条数 → 再砍世界书 → 最后才动角色卡
```

降级顺序有讲究：**永远最后才动角色卡**（人设的根），先从最可再生的（历史原文、检索条数）开刀。

### 5.7 落地优先级（顺序不能反）

- **第一天（无损）**：cache 前缀工程 + 稳定/变化分区 + 状态外置 + 先天/后天记忆分层 → 直接写进上下文组装器。
- **早期（低损）**：渐进披露 + 记忆分层注入 + RAG 精简。
- **中期（有损可控）**：事件记忆图、叙事级摘要、分类压缩、分层摘要。

**每做一个有损优化，就跑一遍 golden set**（记忆忠实度 / 引用正确率 / 风格一致性），指标不掉才保留——让"省 token"从玄学变成可度量的工程。

> 先吃免费的（cache），再吃架构红利（状态外置、记忆分层），最后才碰有损的（压缩、摘要）。

## 6. 角色卡 Schema（数据契约）

> 角色卡是「角色 = 数据而非代码」的载体，也是整个项目**改动成本最高**的部分，因此一次设计到位。

### 6.1 字段总览

**标准字段（兼容酒馆 v2/v3 / chub.ai）**：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `name` | str | ✅ | 角色名 |
| `description` | str | ✅ | 人设主体（身份 / 背景 / 外貌 / 性格综述） |
| `personality` | str | ⬜ | 性格摘要（较短，便于注入） |
| `scenario` | str | ⬜ | 开场情境 |
| `first_mes` | str | ⬜ | **开场白 / 钩子**（从书中名场面改写） |
| `mes_example` | str | ⬜ | **原话 few-shot**，`<START>` 分隔对话轮次 |
| `character_book` | list[LoreEntry] | ⬜ | 世界书条目（关键词触发注入） |
| `creator_notes` | str | ⬜ | 创作者备注 |
| `tags` | list[str] | ⬜ | 标签 |
| `character_version` | str | ⬜ | 版本，默认 `"1.0"` |

`LoreEntry`：`{keys: list[str], content: str, enabled: bool = True, insertion_order: int = 0, case_sensitive: bool = False}`

**立体扩展字段（本项目特有，统一写入 extensions，见 6.2）**：

| 字段 | 类型 | 说明 |
|---|---|---|
| `desire` | str | **欲望**：想要什么（戏剧性来源） |
| `flaw` | str | **缺陷**：性格弱点（内在冲突来源） |
| `secret` | str | **秘密**：不轻易示人的事（渐进披露，见 §14） |
| `timeline` | list[TimelineEvent] | 事件时间线（先天记忆载体） |
| `relations` | list[Relation] | 关系（**单向存储**，见 6.3） |
| `plot_nodes` | list[PlotNode] | 剧情节点（**MVP 阶段留空**，属第二阶段） |

`TimelineEvent`：`{id, order, summary, actors: list[str], chapter: str \| None, quote: str \| None}`
`Relation`：`{**from_**: str, to: str, kind: str, description: str, evolution: list[str] = []}`
`PlotNode`：`{id, order, title, description, chapter: str \| None, branch: str = "canon"}`

### 6.2 v2/v3 导入导出映射策略

**导出（`to_tavern_v2()`）**——标准字段直出到 `data.*`，立体扩展统一收进 `data.extensions.booksoul`：

```json
{
  "spec": "chara_card_v2",
  "spec_version": "2.0",
  "data": {
    "name": "沈知舟",
    "description": "...",
    "personality": "...",
    "scenario": "...",
    "first_mes": "...",
    "mes_example": "<START>\n{{user}}: ...\n{{char}}: ...",
    "creator_notes": "...",
    "tags": ["古风", "师兄"],
    "character_version": "1.0",
    "extensions": {
      "booksoul": {
        "desire": "...", "flaw": "...", "secret": "...",
        "timeline": [], "relations": [], "plot_nodes": []
      }
    }
  }
}
```

**三条关键决策**：
1. **标准字段直出** `data.*`——满足 chub.ai / 酒馆规范，保证外部工具可用。
2. **立体扩展统一收进 `data.extensions.booksoul`**——外部工具忽略未知 extension 不影响兼容；自家内核只读这一处。
3. **导入要兼容两种格式**：① 有 `spec: chara_card_v2` / `chara_card_v3` 包装的（chub.ai 主流）；② 无包装的裸字段格式（老卡）。自动识别，缺失 `extensions.booksoul` 时立体字段取默认值（不报错）。

### 6.3 relations 方向约定

- **单向存储**：每条关系记为 `from → to`（如 `沈知舟 → 女主, kind="师兄妹"`）。
- **反查用索引**：查「谁和 B 有关系」时用 `to` 建索引查询，**不做双向冗余**。
- **理由**：双向冗余会让 `A→B` 与 `B→A` 的描述产生冲突（各自视角不同），且更新成本翻倍。**方向性本身就是信息**（"他爱她"≠"她爱他"）。

### 6.4 Pydantic 落地骨架

```python
from pydantic import BaseModel, Field

class CharacterCard(BaseModel):
    # ── 标准字段 ──
    name: str
    description: str
    personality: str = ""
    scenario: str = ""
    first_mes: str = ""
    mes_example: str = ""
    character_book: list[LoreEntry] = Field(default_factory=list)
    creator_notes: str = ""
    tags: list[str] = Field(default_factory=list)
    character_version: str = "1.0"
    # ── 立体扩展 ──
    desire: str = ""
    flaw: str = ""
    secret: str = ""
    timeline: list[TimelineEvent] = Field(default_factory=list)
    relations: list[Relation] = Field(default_factory=list)
    plot_nodes: list[PlotNode] = Field(default_factory=list)
    # ── 元数据 ──
    source_book: str | None = None      # 来源小说
    extraction_meta: dict = Field(default_factory=dict)  # 抽取依据/置信度

    def to_tavern_v2(self) -> dict: ...
    @classmethod
    def from_tavern(cls, raw: dict) -> "CharacterCard": ...
```

**约定**：字段命名 snake_case（内部）；导出时按 v2 规范（同为 snake_case，无冲突）。所有立体字段给默认值，保证「导入外部卡」时缺字段不报错。

## 7. 技术选型决策（含理由）

| 层 | 选型 | 理由 |
|---|---|---|
| 语言 | Python 3.12+ | 生态最全 |
| 主力模型 | DeepSeek 主力（Flash 系列），豆包做情感高光候选 | 中文强 + 便宜 + 开源退路；**2026-09-10 缓存降价后命中输入 0.02 元/百万（1/50），对长对话是决定性优势**；豆包情感陪伴最强但绑字节。LiteLLM 路由，切换=改配置 |
| 抽取/剧情模型 | 便宜模型粗扫 + 强模型精抽 | 长文本成本 vs 精度；批量任务调度到闲时 |
| 编排 | 自研为主，不用 LangChain / LangGraph 作为核心 | 在线是长对话非工作流；且**框架的自动组装会挡住 cache 前缀控制**（见 §5.2）。离线管道预留 LangGraph 接入点，遇到「长任务重跑成本」痛点再引入 |
| 记忆 | 自研三层契约（canon/heuristic/reflex）+ pgvector + 结构化 timeline/relations | 可控、简历亮点、省抽取成本；借鉴 chronicler，不绑 mem0 |
| 知识/RAG | 小说文本检索（pgvector + 轻量分块） | 对话按需注入书中剧情 |
| 上下文 | 自研 Context Assembler（配额 + 降级） | 见 §5.6；token 优化的落点 |
| 架构模式 | 在线轻（ReAct + Plan-Execute）、离线重（并行 map + 异构专家 + 校验） | 见 §11.2；「不该用 agent 的地方不用」是核心判断 |
| 多 agent | 离线：并行 map（按角色）+ 异构专家（关系/剧情）+ Verification 校验；在线：刻意保持单 agent | 见 §11.3；多角色群聊 + 可见性 ACL 后置 |
| 安全 | guardrails + prompt injection 防护 + 免责声明 | 个人阶段先基础防护 |
| 工具 | 记忆引擎后续包成 MCP server（mcp SDK v2） | 可复用、生态关联 |
| 评估 | 记忆忠实度 / 引用正确率 / 风格一致性（DeepEval/promptfoo + pytest golden set） | 无标准答案任务的评估；也是 token 优化的守门人 |
| 前端 | Chainlit（MVP）→ AG-UI/自研 | 快、够用 |
| 观测 | OTel 埋点先行，Langfuse 后置 | 先埋点零改造 |
| 部署 | Docker compose | 后置 |

## 8. 关键设计判断（几个反直觉结论）

1. **演技七分靠 prompt，三分靠模型**：角色扮演的"像"主要来自角色卡写得好 + 原话 `mes_example` few-shot，模型是第二位的、可换的。
2. **记忆分两半，选型别一刀切**：**先天记忆**（书里剧情，来自剧情理解管道，结构化、你有而 mem0 没有）走自研复用；**后天记忆**（聊出来的新事）MVP 先自研三层契约 + 异步抽取，遇瓶颈再评估 mem0。
3. **可插拔 = 角色是数据不是代码**：换卡 = 换一份 JSON，内核零改动；同时对齐酒馆事实标准，双向生态（导入 chub.ai 现成卡 + 导出分享）。
4. **冷启动靠"内容导入"而不是"社区"**：用户自带 txt，无需 UGC 冷启动；且情感代入更强。
5. **省 token 的顺序不能反**：cache（无损）→ 架构（状态外置/记忆分层）→ 压缩摘要（有损）。上来就搞压缩，等于用效果换 token，得不偿失。
6. **multi-agent 按「角色」切，不按「字段」切**：同实体的多个属性读的是同一批文本，按字段切会让每个 agent 都读一遍全书（上下文重复消耗 + 理解不一致）；而跨实体的关系/时间线**塞不进任何单个角色 agent**，必须由全局异构专家处理。详见 §11.3。
7. **并行 map ≠ multi-agent**：同一抽取器处理 N 个角色是并行 map（同质 agent），只有「关系分析 / 校验」这类需要不同视角和工具的子 agent 才是真 multi-agent。术语要分清，否则面试会被追问穿。

## 9. 技术难点与对策（简历技术点）

| 难点 | 对策 |
|---|---|
| 百万字长文本 | 分层：便宜模型先粗扫定位人物/关键章节，再精抽片段 |
| 人物信息散落全书 | 多遍抽取 + 聚合 + 冲突消解（区分早期/后期人设） |
| 抽取成本 | 模型分级 + 一次性离线构建 + 闲时调度 + 缓存 |
| 角色成长/矛盾 | 时间线建模，区分「早期人设 vs 后期人设」 |
| 人设不崩 | 原话 `mes_example` few-shot + 风格约束 + 反幻觉校验（grounding） |
| 长对话 token 爆炸 | Context Assembler + 事件记忆图 + cache 前缀工程（见 §5） |

## 10. 风险与边界

- **版权**：个人自用/学习/简历阶段无碍；商业化需处理（优先公版书 + 用户自有文本）。
- **合规**：以"工具"定位为主（用户自带文本/自接模型）；大众产品化再补内容审核与未成年防护。
- **成本**：离线抽取一次性成本 + 对话持续成本，已有 §5 的 cache/状态外置/分层策略对冲。
- **vibe coding 边界**：AI 写代码，但**架构、数据契约、评估指标由人把关**（cache 前缀排版尤其容易被 AI 写崩）。

## 11. Agent 架构模式选型

> 本章回答两个问题：**每个环节该用什么模式（ReAct / Plan-Execute / Multi-agent / Workflow）**，以及 **multi-agent 到底该怎么切**。这两条是「在线轻 / 离线重」架构的具体落地。

### 11.1 六层认知地图（避免层级混淆）

| 层级 | 解决什么问题 | 代表 | 本项目立场 |
|---|---|---|---|
| ① 概念层 | agent 是什么、边界在哪 | agent 四要素、workflow vs agent | 判据：**控制流由 LLM 运行时决定**才是 agent |
| ② 架构模式层 | agent 怎么组织 | ReAct / Plan-Execute / Multi-agent | **核心决策，见 11.2 / 11.3** |
| ③ 能力组件层 | agent 需要哪些能力 | 记忆、RAG、工具调用、上下文工程、评估 | **主战场（技术叙事来源）** |
| ④ 框架层 | 用什么库写 | LangChain / LangGraph / Pydantic AI | 自研为主；仅离线管道预留 LangGraph |
| ⑤ 协议层 | 不同 agent / 工具怎么互通 | MCP / A2A / AGENTS.md | 记忆引擎做成 MCP server |
| ⑥ 基础设施层 | 跑在哪、怎么服务 | vLLM / Docker / Temporal / FastAPI | 够用即可：FastAPI + pgvector + Docker |

### 11.2 逐环节模式选型

| 环节 | 选什么模式 | 为什么 | 成本性质 |
|---|---|---|---|
| TXT 解析 / 清洗 | 确定性代码（非 agent） | 无决策空间，用 agent 是纯浪费 | 一次性 |
| 角色识别（粗扫） | Workflow（固定两阶段） | 策略固定：逐章扫人名 → 汇总候选 | 一次性 |
| 角色个体属性精抽 | **并行 map**（同一抽取器 × N 角色） | 上下文隔离 + 可并行 + 省 token | 一次性 |
| 全局关系 / 剧情理解 | **异构专家**（跨角色视角） | 关系天然跨实体，塞不进单个角色 agent | 一次性 |
| 聚合 + 校验 | **Verification 专家** | 防误差雪崩（见 11.5） | 一次性 |
| 在线对话 | **ReAct**（轻量 tool-calling loop） | 反应式，无长期计划；80% 轮次不该调工具 | 持续（要省） |
| 剧情推进 | **Plan-and-Execute** | 需要全局视野，否则剧情走散；用户岔开就 replan | 持续（低频） |
| 记忆抽取 / 晋升 | 异步决策任务（非在线循环） | 绝不能阻塞对话延迟 | 持续（异步） |

**核心判断：不该用 agent 的地方别用。** TXT 解析是纯代码、角色识别是固定 workflow、在线对话是轻量 ReAct——**真正需要 multi-agent 的只有「角色精抽」链路**，且它落在**离线**（一次性成本）；在线部分保持轻量（持续成本）。

### 11.3 multi-agent 切分方案（关键设计决策）

**问题**：multi-agent 应该「按角色切」（每个 agent 负责一个角色）还是「按字段切」（每个 agent 负责一种属性）？

**结论：按角色切（A）负责「量」，异构专家（B）负责「质」；绝不按字段切。**

| | 按「角色」切（A） | 按「字段」切（B） |
|---|---|---|
| 切分维度 | 实体（人物） | 任务类型（性格 / 关系 / 风格） |
| 上下文效率 | ✅ 每个 agent 只读该人物相关段落 | ❌ 每个字段都读一遍全书，重复消耗 |
| 并行度 | 人物数量（动态） | 字段种类（固定 3–5 个） |
| 关系 / 时间线 | ❌ 跨角色，塞不进任何单个 agent | ⚠️ 能抽，但缺全局一致性 |
| 本质 | **并行 map**（同质 agent，≠ multi-agent） | **异构专家**（真 multi-agent） |
| 成本 | 低 | 高（重复读全文 + 多轮交互） |

**四阶段架构**：

```
阶段 1  识别主要人物          → workflow（粗扫 → 候选列表）
阶段 2  按角色并行抽个体属性   → 并行 map（同一抽取器 × N 角色）★ 省 token 主力
阶段 3  全局关系与剧情分析     → 异构专家（跨角色视角；关系图 / 事件时间线 / 剧情节点）★ 真 multi-agent
阶段 4  聚合 + 校验           → Verification 专家（对照原文验证）★ 防误差雪崩
                              → 立体角色卡
```

**切分的正确标准**：

| 判断 | 结论 |
|---|---|
| 同实体的多属性，读同一批文本 | **不要切**，一个 agent 一次抽完 |
| 不同实体，各自独立、各读局部文本 | **切开并行**（map），省时省 token |
| 跨实体的全局结构（关系 / 时间线） | **必须单独**，需要全局视角 |
| 需要不同工具或不同视角（校验、反幻觉） | **切开且异构**（真 multi-agent） |

**⚠️ 术语纪律**：阶段 2 的"同一抽取器 × N 角色"是**并行 map**，不是严格意义的 multi-agent（agent 同质）。简历与文档里要区分表述，别把 map 说成 multi-agent——面试官会追问。

### 11.4 市面 multi-agent 三大范式（参考借鉴）

| 范式 | 在干什么 | 可借鉴的模式 | 代表 |
|---|---|---|---|
| ① Supervisor-Worker | 主管拆任务给多个子 agent，各搜/读/分析，主管综合 | **fresh-context subagents**、orchestrator synthesis、verification role、rainbow deploy | Anthropic 多 agent 研究系统、DeerFlow |
| ② SOP 角色分解 | 模拟团队按固定流程交付产物（产品→架构→工程→QA） | role prompts 编码 SOP、**structured artifact handoffs**、communicative dehallucination | MetaGPT、ChatDev、CrewAI |
| ③ 群体规模 | 大量 agent 在网络中互动，涌现社交 / 经济行为 | local ReAct loop、agent 社交图、**prompt-injection 威胁模型** | Generative Agents、Moltbook |

**本项目属于范式 ①**，且要吸收范式 ② 的「结构化产物交接」（阶段间传 Pydantic 对象，不传聊天记录）。

### 11.5 生产级 multi-agent 三件套与误差雪崩

**误差雪崩（数学事实）**：单步准确率 95% 时——

- 10 步后：`0.95¹⁰ ≈ 59.9%`
- 20 步后：`0.95²⁰ ≈ 35.9%`

multi-agent 会把链路拉得更长，所以**它天然更容易崩**。由此得出生产级 multi-agent 的三件必需品（缺一即赌博）：

1. **Verification（验证角色）**——必须有机制检查上游产出，防误差传导
2. **Cost accounting（成本核算）**——多 agent 成本是单 agent 的 10–20 倍，不核算即失控
3. **Rainbow deploy（分批灰度）**——别一次性全量上线

**已知的 agent 通病（要针对性设防）**：

| 通病 | 本项目的对策 |
|---|---|
| 误差传导（0.95ⁿ） | 阶段 4 加 Verification 专家 + 关键步骤 JSON Schema 卡死 |
| 反思死循环（同逻辑重试耗尽 token） | 设重试上限 + 换策略才允许重试 |
| Lost in the Middle（长上下文中间丢失） | 检索式注入（只给相关片段），不硬塞全文 |
| 记忆碎片割裂（多 agent 靠 RAG 同步有随机性） | 阶段间结构化交接（Pydantic），不靠语义检索同步 |
| 权限悖论 | 离线管道只读原文；在线写操作（记忆）限流可回滚 |
| Token 绞肉机 | 阶段 2 用并行 map（便宜）；异构专家只用在阶段 3/4 |

## 12. 待办与待定

### 12.1 待定项（需要讨论后决策）

1. 项目正式命名（暂定"书魂 BookSoul"）。
2. 首发题材/流派（乙女 / 悬疑 / 通用）。
3. 主力模型最终定档（DeepSeek Flash 已因缓存降价占优，待玩家体感确认"演得像不像"）。
4. 后天记忆：确认 MVP 自研、后续再评 mem0 的路径。
5. 事件记忆图 vs 分层摘要的取舍（两者都做还是先做一个）。
6. **并行 map 的并发度与成本上限**：一本网文可能识别出几十个人物，是全部精抽还是只抽主要角色？（涉及成本，见 §11.2）
7. **Verification 环节的形态**：用确定性规则校验（字段值能否在原文找到）还是独立校验 agent？（前者便宜可测，后者更全面）
8. **剧情推进的 replan 机制**（对应 §11.2 的 Plan-and-Execute）：用户岔开后，是重规划剩余全部节点，还是只调整下一步？

### 12.2 文档待补清单（更新于 v0.4）

| 优先级 | 待补内容 | 状态 |
|---|---|---|
| **P0** | 记忆三层契约专章 | ✅ 已补 → **§13** |
| **P0** | 人格设计与 mood 情绪系统 | ✅ 已补 → **§14** |
| **P0** | MVP 范围定义 | ✅ 已补 → **§15** |
| P1 | 角色卡 Schema 完整化 | ✅ 已补 → **§6.1–6.4** |
| P1 | 数据存储 schema | ✅ 已补 → **§13.6**（MVP 决策：用文件，不上数据库） |
| P1 | 剧情推进机制 | ⏸ 延后（不在 MVP 范围，属第二阶段） |
| P2 | 「调教」交互设计 | ⏸ 延后 |
| P2 | 关系状态机 | 🟡 部分（§14.4 给了 mood；好感度/阶段模型延后） |
| P2 | 评估体系具体方法 | 🟡 部分（§15 给了验收标准；打分方法在 MVP 阶段 10 落地） |
| P2 | 用户 persona | ⏸ 延后（酒馆有，MVP 不做） |
| P3 | 成本模型量化 | ⏸ 延后（MVP 阶段 10 量化） |
| P3 | 遗忘机制 | 🟡 部分（§13.3 给了淘汰触发；衰减策略 MVP 后细化） |

### 12.3 已修正的问题（评审闭环）

- ✅ §5.3 / §5.4 的 RAG top-k 口径已统一为「初定 3~5，由评估确定」
- ✅ §4 数据流已与 §11.3 的四阶段对齐（并互相引用）
- ✅ §2 的悬空引用已改为诚实的「设计待补」标记
- ✅ §6 已从「字段清单」补全为完整数据契约（含 Pydantic 骨架与 v2/v3 映射）
- ✅ v0.4 补齐 §13 / §14 / §15 三章，待补清单的 P0 项已清零

---

## 13. 记忆三层契约

> 记忆是产品的核心体验（"它记得我"）与简历的主打卖点，也是最难做对的一块。核心问题不是"怎么存"，而是**"什么该记、记多牢、什么时候能想起来"**。

### 13.1 三层定义

| 层 | 是什么 | 例子 | 生命周期 | 每轮注入? |
|---|---|---|---|---|
| **canon**（正典） | 确认无疑的事实 | 用户叫小明；沈知舟是女主师兄 | 永久 | 可常驻（精简列表） |
| **heuristic**（推断） | AI 推断、待验证 | 用户似乎不喜欢被叫"明哥" | 反复验证后晋升或淘汰 | 仅按需检索 |
| **reflex**（反射） | 临时场景状态 | 刚才我们在吵架；当前 mood | 短期，会过期 | 仅当前会话 |

### 13.2 写入契约（什么信息以什么级别写入）

| 来源 | 默认级别 | 说明 |
|---|---|---|
| 用户明确陈述的事实（"我叫小明"） | **heuristic** | 需再确认才升 canon |
| 用户重复确认 / 用户主动更正 | **canon** | 直接可信 |
| AI 的推断（"他似乎喜欢……"） | **heuristic** | **必须显式标记为推断**，不能伪装成事实 |
| 场景临时状态（情绪、当前话题） | **reflex** | 不跨会话 |
| 寒暄、玩笑、过渡语 | **不写入** | 噪音不入库 |

> **三条铁律**：① **默认级别是 heuristic 而不是 canon**——避免噪音污染长期记忆；② **AI 推断必须显式标记**；③ **寒暄不入库**——否则记忆库会被垃圾淹没，检索质量断崖下跌。

### 13.3 晋升与淘汰契约

| 转变 | 触发条件 |
|---|---|
| 不写入 → reflex | 当前会话需要（临时记着） |
| reflex → heuristic | 同一信息在**不同会话**出现 ≥ 2 次 |
| heuristic → canon | 用户**明确确认**，或出现 ≥ 3 次且**从未被用户纠正** |
| 任意层 → 淘汰 | 与 canon 冲突且被用户纠正；或长期未命中（衰减，MVP 后细化） |

**反向通道（同样重要）**：用户能**纠正记忆**（"不对，我不喜欢狗"）→ 触发冲突检测 + 降级原条目。**没有纠正通道的记忆系统会越记越错。**

### 13.4 读取契约（注入策略）

| 层 | 注入方式 | 理由 |
|---|---|---|
| canon | 相关条目**可常驻**（精简），或按检索命中注入 | 最重要、最可靠 |
| heuristic | **仅按需检索**，不常驻 | 未经确认，常驻有风险 |
| reflex | 仅当前会话，不跨会话 | 是临时状态 |

- **同等相关性下排序**：canon > heuristic > reflex
- **冲突时**：canon 优先，但 heuristic **保留**（它可能代表"正在变化"）
- **预算**：记忆注入仅 0.5–1K token（见 §5.4），所以只注入 top-k

### 13.5 先天 / 后天记忆的冲突消解

**问题**：书上设定"讨厌甜食"（先天），聊久了角色"爱吃甜食"（后天）——谁赢？

**规则：不覆盖，而是分层 + 标记时间线。**

```
先天：讨厌甜食（来自 第 3 章 · 出厂设定）
后天：爱吃甜食（来自 2026-09-11 与用户的对话 · 关系深入后）
```

角色应当表现为：**"我原来不喜欢，但后来……"**——这本身就是人格深度（人会变），而不是 bug。

实现要点：
1. **先天记忆不可变**（是角色的"出身"），永远保留。
2. 后天变化作为**独立条目 + 时间戳**存储，**绝不覆盖**先天。
3. 话题相关时**两者都注入**，让模型自行组织表达。
4. 检测到矛盾时打 `conflicts_with` 标记，注入时提示模型"这是一处变化"。
5. **例外**：用户明确要求覆盖（"忘记这个设定"）时遵循用户——用户是最终裁决者。

### 13.6 存储 schema

**MVP 决策：不上数据库，用文件。** 理由：零运维、验证优先、可直接查看调试。

| 数据 | 位置 | 形式 |
|---|---|---|
| 角色卡 | `data/cards/{character_id}.json` | 单文件（§6 的 Schema） |
| 会话 | `data/sessions/{session_id}.jsonl` | append-only |
| 记忆 | `data/memory/{character_id}.jsonl` | append-only，每行一条 |
| 索引 | 内存加载 + jieba 分词 + 简单打分 | 不做 embedding |

**记忆条目结构**：

```json
{
  "id": "mem_xxx",
  "character_id": "char_xxx",
  "tier": "heuristic",
  "source": "dialogue",
  "content": "用户小时候养过一只叫豆豆的狗，走丢了",
  "quote": null,
  "created_at": "2026-09-11T10:00:00+08:00",
  "hits": 3,
  "corrected": false,
  "conflicts_with": []
}
```

`source` 取值：`book`（先天，来自离线抽取）｜`dialogue`（后天，来自对话）。

**未来 Postgres 迁移草案（第二阶段）**：表 `users / characters / sessions / messages / memories / events / relations`；`memories` 增加 `embedding vector` 字段，用 pgvector 做语义检索（替换 jieba 关键词检索）。

---

## 14. 人格设计与 mood 情绪系统

### 14.1 核心主张

> **让 AI 像人，不是让它"更懂你"，而是让它"更像一个不依赖你而存在的独立个体"。**

现在几乎所有产品都在优化"贴心、顺从、有求必应"，**但那个方向恰恰通向"工具感"**。灵魂来自**他者性**——它是一个主体，不是你的客服。

### 14.2 反面清单：让人显得像工具的做法

| 常见做法（越做越不像人） | 为什么错 | 正确方向 |
|---|---|---|
| 永远顺从、永远赞同 | 客服感 / 谄媚感 | 有自己的立场，会不情愿、会反对 |
| 永远热情体贴 | 推销员感 | 有情绪波动：会低落、会烦躁、会心不在焉 |
| 有问必答、从不回避 | 搜索引擎感 | 有边界，有权不回答、有不想聊的话题 |
| 完美符合设定、永远冷静 | 木偶感 | 有内在冲突、会犹豫、会改口 |
| 什么都聊、什么都接 | 没有自我 | 有偏好、有厌恶 |
| 立刻亲密、无条件接纳 | 没有关系深度 | 关系循序渐进：有试探、有误解、有和解 |

### 14.3 人类特征 → 技术实现

| 人类特征 | 技术实现 | 本项目载体 |
|---|---|---|
| 有自己的意志 | `desire` + 边界；prompt 明确允许拒绝与不情愿 | 角色卡 `desire` + 边界字段 |
| 内在冲突 | `flaw` + 矛盾设定（想靠近又害怕） | 角色卡 `flaw` |
| 秘密与隐私 | `secret` + 按关系阈值渐进披露 | 角色卡 `secret` + 关系状态 |
| **情绪波动** | **mood 状态机**（见 14.4） | 新增 mood 状态 |
| 主观视角 | prompt 要求"我认为"而非"答案是" | prompt 模板层 |
| 记忆的主观性 | 印象式记忆（允许模糊、偏差） | heuristic 层天然允许不确定 |
| 主动性 | 叙事调度器：自己想说才说 | 第二阶段 |
| 关系节奏 | 好感度 / 阶段门槛，不一步到位 | 第二阶段 |
| 有限与脆弱 | 允许说"我做不到""我累了" | prompt + 拒绝全能设定 |
| 不可预测 | 温度 + 随机事件 + 角色自己的偏好 | 第二阶段 |

### 14.4 mood 状态机（当前设计缺失的关键）

**设计取舍**：不做多维情绪模型（复杂且难调），用 **主情绪（枚举）+ 强度（0–100）**。

**情绪枚举（MVP 建议 5 种）**：`calm`（平静）｜`warm`（温暖/愉悦）｜`annoyed`（烦躁）｜`low`（低落）｜`guarded`（戒备）

**状态结构**：

```json
{
  "character_id": "char_xxx",
  "mood": "guarded",
  "intensity": 40,
  "reason": "用户刚才问起他的过去",
  "baseline": "calm",
  "updated_at": "2026-09-11T10:00:00+08:00"
}
```

**触发规则**：

| 触发源 | 影响 |
|---|---|
| 剧情事件（触及书中敏感话题） | 按角色设定触发（如提到"家人"→ `guarded`） |
| 用户言行（冒犯 / 亲近） | 负面 → `annoyed` 强度 +；正向 → `warm` 强度 + |
| 时间/轮次衰减 | 每 N 轮向 `baseline` 回落 |
| 基线性格 | 每个角色有 `baseline`（来自 `personality` / `flaw`） |

**对生成的影响**：转成**一句简短的语气指令**注入 prompt（注意 token，不要长篇）：
- `annoyed(70)` → "你现在心情烦躁，说话简短、带刺，不太想多聊。"
- `warm(60)` → "你现在心情不错，语气放松，愿意多说两句。"

**⚠️ 与 cache 的关系（易错点）**：mood 是**变化的**，所以**必须放在动态区**——绝不能进稳定前缀，否则每次变化都会击穿 prompt cache（§5.2）。这也是「状态外置」（§5.3）的具体应用。

**层级归属**：mood 属于 **reflex 层**（临时状态，不跨会话，见 §13.1）。

**验收方式**：同一句用户输入，在 mood 不同时回复应有**可感知的差异**。

### 14.5 三条设计铁律

1. **角色要有自己的议程**——有想做的事、想说的话，不总是配合用户。
2. **关系要有阻力**——不是一见如故，而是有试探、有误解、有和解。
3. **保留不可完全占有性**——保留秘密、保留独处、保留不解释的权利。

> 这三条都是**反产品**的（产品化时最想消灭的就是摩擦），但正是它们让 AI 从工具变成"他者"。

### 14.6 本项目的独特优势

- **UGC 角色卡（猫箱 / 酒馆）往往扁平**："完美的霸道总裁""无条件的温柔恋人"——用户捏卡时想的是"我想要什么"，而非"这个人是谁"。
- **文学角色天生立体**：有成长弧光、内在矛盾、不愿示人的一面。

> **产品哲学定位：不是"造一个完美恋人"，而是"让文学角色活过来"。**
> 技术后果：`desire / flaw / secret` 不是"增加戏剧性"的装饰，而是**人格骨架**；`timeline`（成长弧光）是**深度的来源**。

---

## 15. MVP 范围（Walking Skeleton）

### 15.1 两个验证假设

1. **技术假设**：从小说里**能不能抽出"像样的立体角色"**？（抽取质量是地基）
2. **产品假设**：和**"书里角色"**对话，真的比和陌生 UGC 角色更有感觉吗？（差异化成立与否）

> **砍或留的唯一判据：这一项是不是验证这两个假设所必需的？**

### 15.2 范围清单

| | 第一版 | 理由 |
|---|---|---|
| ✅ 必做 | TXT 解析（**限 3–5 万字短篇**） | 输入；短篇足以验证 |
| ✅ 必做 | 角色识别（粗扫找人名） | 找出可扮演对象，最简实现 |
| ✅ 必做 | 核心字段抽取（`name` / `personality` / `first_mes` / `mes_example` + `desire` 或 `secret`） | 验证第一风险点 |
| ✅ 必做 | 角色卡 JSON（v2/v3 兼容） | **数据契约改起来最贵，一次到位** |
| ✅ 必做 | 最简对话（角色卡 + few-shot + 历史 + 检索片段） | 验证第二风险点 |
| ✅ 必做 | 最简记忆（存历史 + 从书里检索段落塞进 prompt） | **没有它就不是"书里角色"，无法验证差异化** |
| ✅ 必做 | CLI（`ingest → identify → extract → chat`） | 能看效果即可 |
| ❌ 砍掉 | 百万字长文本分层策略 | 短篇够验证 |
| ❌ 砍掉 | 并行 map / 并发优化（先串行） | 优化留到确认价值之后 |
| ❌ 砍掉 | 关系图 / 事件时间线 / 异构专家 | 服务于剧情推进，而剧情推进本身就该砍 |
| ❌ 砍掉 | Verification 校验环节 | 第一版人工看即可判断质量 |
| ❌ 砍掉 | 三层记忆契约 / 事件记忆图 | 用最笨的检索实现，验证后再升级 |
| ❌ 砍掉 | Context Assembler 预算降级 | 短对话不会超预算 |
| ❌ 砍掉 | mood / 剧情推进 / 关系状态机 / 多角色 / 调教 UI | 全是"完善"阶段的事 |
| ❌ 砍掉 | 评估体系 / 观测 / 部署 / MCP | 基础设施，验证通过后再补 |

### 15.3 四项验收标准

投放一篇 3–5 万字小说，逐项检查：

| 检查项 | 通过标准 |
|---|---|
| 抽取质量 | 角色卡人设**合理**（不像通用模板），`first_mes` 有代入感 |
| 像不像书里人 | 对话语气**明显不是通用 AI 腔**，能看出原著风格 |
| 记不记得剧情 | 问"你和 XX 什么关系"，**能答对** |
| 钩子 | 开场 3 句内，**要么让人笑，要么让人好奇** |

**四项全过 → 核心假设成立，进入第二阶段**（记忆契约、mood、评估）。**任一项明显不过 → 先解决它，别往前推进。**

### 15.4 失败预案

| 失败项 | 预案 |
|---|---|
| 抽取质量差 | 迭代 prompt + 增加书中原话 few-shot；必要时人工修正后作为样本 |
| 对话不像 | 调角色卡字段与 few-shot；换豆包试"情感高光" |
| **"书里角色"体验无优势** | **核心假设不成立 → 暂停开发，重新评估产品定位**（这是 MVP 最有价值的发现） |

### 15.5 形态与不做的事

- **别做 Web 前端**（CLI / Chainlit 足够，前端属"完善"阶段）
- **别做"通用框架"**（只支持一种输入格式、一种抽取策略）
- **别追求一次跑对**（允许人工介入，如人工确认人物列表）

> **MVP 最大的价值不是"做出了东西"，而是"尽早发现自己错了"。** 这个发现越早越省钱。
