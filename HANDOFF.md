# HANDOFF —— 实现交接简报（书魂 BookSoul）

> **这是什么**：给「实现对话」的项目宪法。**每次开工先读这一页**（约 1 分钟）。
> **不是什么**：这里不放设计细节。设计在 `PROJECT_DESIGN.md`，任务在 `MVP_PLAN.md`。

## 0. 你的角色

你是**实现方**。设计已定稿，你的任务是**按计划实现**，不是重新设计。
遇到设计没覆盖的地方 → **停下来问，不要自己发明**（见 §5）。

## 1. 工作循环（每个阶段都这样）

1. 读本文件 → 确认红线与纪律
2. 读 `MVP_PLAN.md` 的**当前阶段** → 任务清单 + 验收标准
3. 按 **§4 对照表**查 `PROJECT_DESIGN.md` 的相关章节（**不通读**）
4. 实现
5. 按验收标准自检 → **通过才进下一阶段**

## 2. 红线（绝不许做）

| # | 不许 | 为什么 |
|---|---|---|
| 1 | 引入 **LangChain / LangGraph** | 已评估并明确不用（设计 §7、§11.1）；框架的自动组装会挡住 cache 前缀控制 |
| 2 | 引入**数据库 / 向量库 / embedding** | MVP 决策：文件 + jieba 关键词检索（设计 §13.6） |
| 3 | 实现**被砍掉的功能** | MVP 范围见设计 §15.2：mood / 剧情推进 / 关系状态机 / 多角色 / 调教 UI / 评估体系 **都不做** |
| 4 | **提前做优化** | 并发、预算降级、cache 工程、分层摘要 → 全部留到验证通过后 |
| 5 | 做 **Web 前端** | CLI 足够（设计 §15.5） |
| 6 | 在业务代码里**直接调 LLM SDK** | 必须走 `llm/client.py`（便于换模型、统计成本） |
| 7 | 把 **API Key 写进代码或提交 git** | 读环境变量 `DEEPSEEK_API_KEY` |

## 3. 人工把关点（不确认不得往下走）

| 阶段 | 为什么必须停 |
|---|---|
| **阶段 1 数据契约** | 角色卡 Schema 是**改动成本最高**的部分（设计 §6） |
| **阶段 4 角色抽取** | **MVP 第一风险点**：抽不出"像样的角色"，整个项目不成立 |

产出后**先给人看**，确认后再继续。

## 4. 阶段 → 必读章节对照表

| 阶段 | 必读 |
|---|---|
| 1 脚手架与数据契约 | §6（全章） |
| 2 TXT 解析与分章 | ——（纯代码，无需查设计） |
| 3 角色识别 | §9 |
| 4 角色抽取 🔴 | §6.1、§9、§11.3、§15.3 |
| 5 角色卡组装与校验 | §6.1、§6.2 |
| 6 对话运行时 🔴 | §5.2（cache 前缀工程）、§14.3 |
| 7 最简记忆 | §5.4、§13.1–13.4 |
| 8 CLI 界面 | §15.5 |
| 9 端到端验收 | §15.3 |
| 10 评估基线 | §5.7、§15.3 |

> §编号均指 `PROJECT_DESIGN.md`。

## 5. 设计空白清单（遇到就停下问）

以下**尚未设计**。实现时若需要 → **停下来问，不要自己发明**：

- 抽取 Prompt 的**具体写法**（正在设计，阶段 4 前会补齐）
- 对话 system prompt 的**模板结构**（正在设计，阶段 6 前会补齐）
- 关键词检索的**打分 / 排序规则**
- 四项验收标准的**具体打分操作**（怎么算"过"）
- 剧情节点的推进机制、关系状态机模型、调教交互形态、成本量化方法（**均属第二阶段，MVP 不做**）

## 6. 全局工程约定

- 所有 LLM 调用走 `llm/client.py`（重试 / 超时 / usage 记录）。
- 所有中间产物**落盘缓存**（识别结果、抽取结果），避免重复烧 token。
- LLM 返回的结构化数据 → **Pydantic 校验 + 失败重试一次**；仍失败则**报错退出**，不静默吞掉。
- 路径固定（见 `MVP_PLAN.md` §1 目录结构），便于后续阶段复用与人工检查。
- 中文文本先做编码探测（utf-8 / gbk / gb18030）。

## 7. 完成定义

- **单阶段**：以 `MVP_PLAN.md` 该阶段的「验收标准」为准，**不是"跑通了"**。
- **MVP 整体**：以设计 §15.3 的**四项标准**为准：

| # | 标准 |
|---|---|
| ① | 抽取质量：人设合理（不像通用模板），`first_mes` 有代入感 |
| ② | 像不像书里人：语气**明显不是通用 AI 腔** |
| ③ | 记不记得剧情：能答对书中关系 / 事件 |
| ④ | 钩子：开场 3 句内，要么让人笑，要么让人好奇 |

**四项全过 → 才进第二阶段；任一项不过 → 停在原地解决。**

## 8. 三条最容易被忽略的设计原则

1. **演技七分靠 prompt，三分靠模型**（§8.1）——角色不像时，**先改角色卡和 few-shot**，别急着换模型。
2. **稳定前缀必须逐字符不变**（§5.2）——绝不要把时间戳、检索结果插进「角色卡 / few-shot」之间，会击穿 prompt cache。
3. **mood / 检索结果 / 剧情状态必须放动态区**（§5.3、§14.4）——它们是变化的，进稳定前缀会毁掉缓存。
