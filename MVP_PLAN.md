# 书魂 BookSoul —— MVP 十阶段实施计划（vibe coding）

> 本计划是 `PROJECT_DESIGN.md` §15「MVP 范围」的落地执行版。
> **目标**：用最低成本验证两个假设 —— ① 从小说能抽出"像样的立体角色"；② 和"书里角色"对话比陌生 UGC 角色更有感觉。
> **本计划只覆盖 MVP**（Walking Skeleton）。记忆三层契约、mood、剧情推进、评估体系等属于第二阶段，见 `PROJECT_DESIGN.md` §13/§14/§12。

---

## 0. 为什么是这十个阶段（"不为了分而分"）

阶段划分依据四条原则，而不是凑数：

1. **每个阶段结束都有可运行 / 可检查的产物** —— 不存在"写了三天但什么都跑不起来"的阶段。
2. **两个核心风险点独立成阶段** —— 阶段 4（抽取质量）与阶段 6（对话体验）是整个项目成败所在，必须能被单独验证、单独失败。
3. **纯代码环节与 LLM 环节分开** —— 阶段 2（解析，纯代码）与阶段 3（识别，首个 LLM 环节）风险性质完全不同，混在一起会让问题难以定位。
4. **严格按依赖顺序，且不提前做优化** —— 并发、缓存工程、预算降级都留到验证通过之后。

> 判断某个任务该不该进本计划，只有一个问题：**"它是验证上面两个假设所必需的吗？"** 不是 → 不做。

---

## 1. 技术约定（decision-complete）

| 项 | 决策 | 理由 |
|---|---|---|
| Python | **3.14.3**（本机已装） | 环境现成 |
| 包管理 | **pip + venv**（不用 uv，本机未安装） | 不引入额外工具链 |
| 依赖 | `pydantic`、`litellm`、`typer`、`rich`、`jieba`、`pytest` | 最小集 |
| **不引入** | 数据库、向量库、embedding、LangChain / LangGraph | MVP 不需要；文件 + 关键词检索足够，且零运维 |
| 模型 | **DeepSeek Flash**（LiteLLM 路由） | 缓存降价 1/50，长对话成本最优；换模型=改配置 |
| 存储 | **文件**（`data/` 下 JSON / JSONL） | 零运维、易调试、可直接查看 |
| 检索 | 人名 + 关键词（jieba 分词 + 简单打分） | 避免引入 embedding 模型 / API |
| API Key | 读环境变量 `DEEPSEEK_API_KEY` | 不写进代码、不进 git |

### 目录结构（与十阶段一一对应）

```
D:\story\
├── PROJECT_DESIGN.md
├── MVP_PLAN.md                  ← 本文件
├── pyproject.toml
├── .gitignore
├── README.md
├── src/booksoul/
│   ├── __init__.py
│   ├── config.py                # 环境变量 / 模型名 / 路径
│   ├── llm/client.py            # 阶段3：LiteLLM 封装（重试/超时/usage）
│   ├── schema/
│   │   ├── character.py         # 阶段1：CharacterCard 等（§6.4）
│   │   └── tavern.py            # 阶段1：v2/v3 导入导出
│   ├── ingest/parser.py         # 阶段2：TXT 解析分章
│   ├── extract/
│   │   ├── identify.py          # 阶段3：角色识别
│   │   └── extract.py           # 阶段4：角色抽取
│   ├── assemble/builder.py      # 阶段5：组装 + 校验
│   ├── runtime/
│   │   ├── dialogue.py          # 阶段6：对话循环
│   │   └── memory.py            # 阶段7：最简检索
│   └── cli.py                   # 阶段8：CLI
├── tests/
├── data/{novels,cards,sessions,memory}/
└── evals/golden_set.jsonl       # 阶段10
```

### 全局约定（写进 README，所有阶段遵守）

- 所有 LLM 调用统一走 `llm/client.py`，**不允许在业务代码里直接调 SDK**（便于换模型、加成本统计）。
- 所有中间产物**落盘缓存**（识别结果、抽取结果），避免重复花钱。
- LLM 返回的结构化数据一律用 **Pydantic 校验 + 失败重试一次**，仍失败则报错退出（不静默吞掉）。
- 每个阶段的产物路径固定，便于后续阶段复用与人工检查。

---

## 2. 十阶段详解

### 阶段 1：脚手架与数据契约

**目标**：可运行的项目骨架 + 完整数据契约（这是改动成本最高的部分，一次到位）。

**关键任务**
1. 建目录结构、`pyproject.toml`、`.gitignore`、`README.md`。
2. 实现 `schema/character.py`：`CharacterCard` / `LoreEntry` / `TimelineEvent` / `Relation` / `PlotNode`（严格按 `PROJECT_DESIGN.md` §6.4）。
3. 实现 `schema/tavern.py`：`to_tavern_v2()` 与 `from_tavern()`（含 `extensions.booksoul` 映射，见 §6.2）。
4. `config.py`：读 `DEEPSEEK_API_KEY` / `MODEL_NAME` / `DATA_DIR`。
5. 单测：序列化往返、v2 导出结构、**导入兼容两种格式**（有 `spec` 包装 / 裸字段 / 缺 `extensions`）。

**验收标准**：`pytest` 全绿；导出的 JSON 满足 v2 结构；导入缺字段的外部卡不报错。

**依赖**：无　**常见坑**：立体扩展字段必须给默认值，否则导入外部卡直接崩。

---

### 阶段 2：TXT 解析与分章

**目标**：把 `.txt` 变成结构化的章节列表。

**关键任务**
1. `ingest/parser.py`：编码探测（utf-8 / gbk / gb18030）、分章（正则匹配 `第X章/节/回`）、清洗（去空行、去广告行）。
2. 定义 `Chapter{index, title, content}`。
3. 单测：用构造文本测分章与编码探测。

**验收标准**：对一篇真实短篇，切出的章节数与目测一致；GBK 文件能正确读出。

**依赖**：阶段 1　**常见坑**：中文小说编码混杂；章节标题格式五花八门（要有兜底：找不到标题就按长度切）。

---

### 阶段 3：角色识别（粗扫）

**目标**：找出主要人物候选列表。

**关键任务**
1. `llm/client.py`：LiteLLM 封装（重试、超时、usage 记录）。
2. `extract/identify.py`：**逐章**调 LLM 抽人名（用便宜模型）→ 汇总词频 → 输出候选列表（带出现次数）。
3. 结果落盘 `data/cards/{book_id}/candidates.json`。
4. 单测：mock LLM 响应，测聚合与排序逻辑。

**验收标准**：对样例小说列出的人物，人工检查合理（前 5 名确实是主要角色）。

**依赖**：阶段 2　**常见坑**：LLM 会把人名与地名 / 称谓混淆——prompt 必须限定"只输出人物姓名"。

---

### 阶段 4：角色抽取（🔴 风险点 1）

**目标**：对指定人物抽出结构化、有原文依据的立体字段。

**关键任务**
1. `extract/extract.py`：先检索该人物相关段落（人名 + 上下文窗口），**只把相关段落**拼进 prompt。
2. Prompt 要求输出符合 `CharacterCard` 的 JSON，**并给出原文引用依据**（写入 `extraction_meta.quotes`）。
3. 抽取字段：`name` / `description` / `personality` / `desire` / `flaw` / `secret` / `first_mes` / `mes_example`。
4. Pydantic 校验 + 失败重试一次；结果落盘 `data/cards/{character_id}.json`。

**验收标准**：抽出的字段**像书中人**（不是通用套话）；`first_mes` 有代入感；`mes_example` 是书中风格的原话。

**依赖**：阶段 3　**常见坑**：① 全文塞不下 → 只给相关段落；② 模型输出 JSON 格式错 → 校验重试；③ **抽出通用套话**（最难）→ prompt 必须强制引用原文依据，无依据的字段宁可留空。

---

### 阶段 5：角色卡组装与校验

**目标**：产出可导入酒馆的完整角色卡。

**关键任务**
1. `assemble/builder.py`：把抽取结果组装成完整 `CharacterCard`（补默认值、装配 `character_book` 初版）。
2. 校验：必填字段非空 + `extraction_meta` 中每条结论都有对应引用。
3. 导出 v2 JSON → `data/cards/{character_id}.v2.json`。
4. 人工检查：尝试导入 SillyTavern，或至少通过格式校验。

**验收标准**：产物可被酒馆 / 格式校验器接受。

**依赖**：阶段 4　**常见坑**：`mes_example` 的 `<START>` 轮次格式必须正确。

---

### 阶段 6：最简对话运行时（🔴 风险点 2）

**目标**：能和抽取出的角色多轮对话。

**关键任务**
1. `runtime/dialogue.py`：加载角色卡 → 组装 messages（system: 角色卡 + few-shot；其后为历史）→ LiteLLM 流式 → 多轮循环。
2. **稳定前缀置顶**：角色卡 + few-shot 放最前且逐字符不变，历史在后（为 cache 做准备，见设计 §5.2）。
3. 会话落盘 `data/sessions/{session_id}.jsonl`（append-only）。
4. 支持 `/exit`、`/reset`、`/regenerate`。

**验收标准**：对话语气**明显不是通用 AI 腔**，能看出原著风格。

**依赖**：阶段 5　**常见坑**：① 模型不按角色说话 → few-shot 质量是关键（"演技七分靠 prompt"）；② 历史越长越贵 → 阶段 7 解决。

---

### 阶段 7：最简记忆（检索注入）

**目标**：让角色"记得"书中剧情 —— 这是差异化的核心。

**关键任务**
1. `runtime/memory.py`：用 用户消息 + 角色名 做关键词检索（jieba 分词），从原文取 top-k 段落。
2. 注入到 messages 的**动态区**（绝不能插进稳定前缀）。
3. 历史超过 N 轮时，生成简单摘要替代远期原文。
4. 单测：检索命中的段落确实包含相关剧情。

**验收标准**：能答对"你和 XX 什么关系"、"你们之间发生过什么"。

**依赖**：阶段 6　**常见坑**：① 检索不准 → MVP 先用关键词，不用 embedding；② **注入位置破坏 cache** → 必须放动态区。

---

### 阶段 8：交互界面（CLI）

**目标**：完整可用的命令行流程。

**关键任务**
1. `cli.py`（typer）：四个子命令 `ingest` / `identify` / `extract` / `chat`。
2. `rich` 做终端渲染（进度、角色卡预览、对话样式）。
3. 串联：从放书到对话形成一条完整路径。

**验收标准**：能流畅跑完一轮完整流程（放书 → 抽角色 → 对话）。

**依赖**：阶段 2–7　**常见坑**：**别做 Web 前端**（属"完善"阶段，会拖慢验证）。

---

### 阶段 9：端到端串联与验收

**目标**：对 MVP 核心假设给出明确结论。

**关键任务**
1. 用 1–2 篇真实短篇（3–5 万字）跑完整流程。
2. 按 `PROJECT_DESIGN.md` §15.3 的**四项标准**逐项打分。
3. 记录问题清单：哪个环节最差、最值得先优化。

**验收标准**：四项标准都有明确结论（过 / 不过 / 部分过）。

**依赖**：阶段 8　**常见坑**：容易"跑通了就以为成功了"——必须严格按四项标准评估，而不是凭感觉。

---

### 阶段 10：评估基线与复盘

**目标**：给第二阶段提供依据，同时沉淀简历素材。

**关键任务**
1. 建 `evals/golden_set.jsonl`：10–20 个问题（关于书中剧情 / 关系 / 性格）。
2. 定义三个指标的可操作打分方式：
   - **记忆忠实度** = 答对率
   - **引用正确率** = 引用是否真实存在于原文
   - **风格一致性** = 人工 1–5 分
3. 跑基线，记录分数。
4. 写复盘：发现的问题 + 下一步优先级。
5. **估算成本**：跑一本书多少钱、一次对话多少钱。

**验收标准**：有一份可对比的基线数据 + 下一步清单。

**依赖**：阶段 9　**常见坑**：指标定义必须可操作，否则打不了分、无法对比。

---

## 3. MVP 总验收标准

投放一篇 3–5 万字小说，逐项检查（`PROJECT_DESIGN.md` §15.3）：

| 检查项 | 通过标准 |
|---|---|
| 抽取质量 | 角色卡人设合理（不像通用模板），`first_mes` 有代入感 |
| 像不像书里人 | 对话语气明显不是通用 AI 腔，能看出原著风格 |
| 记不记得剧情 | 问"你和 XX 什么关系"，能答对 |
| 钩子 | 开场 3 句内，要么让人笑，要么让人好奇 |

**四项全过 → 核心假设成立，进入第二阶段**（记忆三层契约 §13、mood §14、评估体系）。
**任一项明显不过 → 先解决它，不要往前推进。**

---

## 4. 假设与边界

- 用户**自备 DeepSeek API Key**（不做密钥管理，读环境变量）。
- 样例小说**格式规整**（有章节标题）、**3–5 万字**（不做长文本分层）。
- **单机单用户、无并发**（不做服务化 / 多租户）。
- **允许人工介入**（如人工确认人物列表、人工检查抽取质量）。

---

## 5. 风险与预案

| 风险 | 预案 |
|---|---|
| 抽取质量差（阶段 4 不过） | 迭代 prompt + 增加书中原话 few-shot；必要时人工修正后作为样本回灌 |
| 对话不像（阶段 6 不过） | 调角色卡字段与 few-shot；换豆包试"情感高光" |
| **"书里角色"体验无优势** | **核心假设不成立 → 暂停开发，重新评估产品定位**（这是 MVP 最有价值的发现） |
| 成本超预期 | 阶段 3 用便宜模型 + 结果落盘缓存；阶段 10 量化后再决定优化 |
| 中文编码 / 章节切分失败 | 阶段 2 加兜底策略（按长度切），不阻塞主流程 |

---

## 6. vibe coding 使用建议

- **一次会话做一个阶段**，把该阶段的"关键任务"作为任务清单交给 AI，用"验收标准"检查产出。
- **阶段 1 和 4 必须人工把关**：阶段 1 是数据契约（改起来贵），阶段 4 是核心风险点（决定项目成立与否）。
- 每个阶段完成后**先跑验收再进下一阶段**，不要攒着一起测。
- 中间产物一律落盘，**失败可重跑**，避免重复烧 token。
